# ========================================
# Docker镜像配置文件
# ========================================
# 修改此文件来配置你的Docker镜像

# 用户配置（与主机用户保持一致，避免权限问题）
USER_NAME=king
USER_UID=501
USER_GID=20

# Docker镜像配置
IMAGE_NAME=aketheia_suite_bridge
IMAGE_TAG=latest
CONTAINER_NAME=aketheia_suite_bridge-container

# 工作空间目录（挂载到容器中的目录）
WORKSPACE_DIR=/Users/king/code/docker-template

# ========================================
# 可选配置（根据需要添加）
# ========================================

# 端口映射（示例）
# PORTS=8080:8080 3000:3000

# 环境变量（示例）
# ENV_VAR1=value1
# ENV_VAR2=value2

# GPU支持（需要nvidia-docker）
# GPU_SUPPORT=false
